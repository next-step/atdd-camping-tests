version: "3.9"
services:
  kiosk:
    build:
      context: ..
      dockerfile: dockerfiles/Dockerfile-kiosk
    container_name: kiosk-app
    ports:
      - "8080:8080"
    environment:
      JAVA_OPTS: "-Xms256m -Xmx512m"